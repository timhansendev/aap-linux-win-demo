---
- name: Enforce linux host configuration
  tasks:
    - name: Apply rsyslog configuration
      src: templates/rsyslog.j2
      dest: /etc/rsyslog.conf
      owner: root
      group: root
      mode: u=rw,g=r,u=r

